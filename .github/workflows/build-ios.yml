name: Build iOS IPA

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'FE/Tempo/**'
      - '.github/workflows/build-ios.yml'

jobs:
  build-ios:
    name: Build Unsigned iOS IPA
    runs-on: macos-15
    timeout-minutes: 60

    steps:
      - name: 1. Checkout repository
        uses: actions/checkout@v4

      - name: 2. Select Xcode 16.2
        run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer

      - name: 3. Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 4. Install Node dependencies
        working-directory: FE/Tempo
        run: npm install

      - name: 5. Expo Prebuild (Generate Native iOS Project)
        working-directory: FE/Tempo
        run: npx expo prebuild --platform ios --clean

      - name: 6. Install CocoaPods Dependencies
        working-directory: FE/Tempo/ios
        run: pod install

      - name: 7. Build Native iOS App Binary
        working-directory: FE/Tempo/ios
        run: |
          xcodebuild \
            -workspace Tempo.xcworkspace \
            -scheme Tempo \
            -configuration Release \
            -sdk iphoneos \
            -derivedDataPath build \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            AD_HOC_CODE_SIGNING_ALLOWED=YES

      - name: 8. Package App into IPA
        working-directory: FE/Tempo/ios
        run: |
          mkdir -p Payload
          cp -r build/Build/Products/Release-iphoneos/Tempo.app Payload/
          zip -r Tempo.ipa Payload
          mkdir -p ../dist
          mv Tempo.ipa ../dist/Tempo.ipa

      - name: 9. Upload IPA to GitHub Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: Tempo-iOS-IPA
          path: FE/Tempo/dist/Tempo.ipa
          retention-days: 14
